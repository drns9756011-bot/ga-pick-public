# 픽견적 가전 구독관 운영 기준

## 상품 반영 기준

- 관리자 화면 `/subscription-products`에서 최신 엑셀을 업로드합니다.
- 사용 시트: `전자랜드`
- 계약기간 `72개월`, 결합유형 `결합없음`, 기본요금만 반영합니다.
- 프로모션, 결합할인, 소상공인 할인, 선납 조건은 반영하지 않습니다.
- 현재 기준 상품 카드: 1,232개
- 관리 방식·관리 등급·방문 주기·콘텐츠 구독 차이는 한 상품의 옵션으로 저장합니다.
- TV 모델 본체 끝의 `S/W`는 스탠드형·벽걸이형으로 판정해 한 상품으로 통합합니다.

## 이미지 기준

- LPLAN 배포자료의 공식 제품 이미지를 모델 코드로 연결합니다.
- 현재 이미지 매칭: 1,232/1,232, 누락 0개
- 개별 이미지 원본은 배포하지 않고 WebP 아틀라스 9개만 서버 자산으로 배포합니다.
- 모델명이 일부 비슷하다는 이유로 다른 용량이나 크기의 이미지를 연결하지 않습니다.
- 이미지 오류 시 공용 가전 이미지를 대신 표시하지 않고 `이미지 준비 중`을 표시합니다.

## 서버 구조

- 공개 조회: `GET /api/subscription-products`
- 관리자 업로드: `POST /api/subscription-products/upload`
- 관리자 상태: `GET /api/subscription-products/status`
- 관리자 API는 `X-Admin-Token` 인증이 필요합니다.
- D1에는 공개에 필요한 상품·옵션·요금·이미지 좌표만 저장합니다.
- 원본 엑셀은 D1, R2, 브라우저 저장소에 저장하지 않습니다.
- 새 세트 저장이 모두 성공한 뒤 활성 세트를 전환하고 이전 세트는 영구 삭제합니다.
- 빈 D1은 노출용 Worker의 비공개 초기 데이터 1,232개를 최초 조회 시 자동 생성합니다.
- 관리자 업로드 데이터가 활성 상태이면 노출용 자동 초기 데이터가 덮어쓰지 않습니다.

## 화면 동작

- 카드 옵션을 변경하면 실제 모델명, 관리 조건, 72개월 월 구독료가 함께 변경됩니다.
- 상세보기 모달에서 기본 정보, 옵션, LG전자 공식 검색, 제휴카드 최대 혜택을 확인합니다.
- 상담 버튼은 상세보기 모달의 마지막 단계에 배치합니다.
- 모든 `/api/` 통신에는 공통 로딩·지연·성공·오류 모달을 표시합니다.

## 보안 확인

- 노출용 ZIP에 `.xlsx`, `.xlsm`, `private-data`, 개별 제품 이미지 폴더를 포함하지 않습니다.
- 공개 정적 구독 상품 JSON을 만들지 않습니다.
- 새 배포마다 ZIP 내부 파일 목록과 SHA-256을 확인합니다.
